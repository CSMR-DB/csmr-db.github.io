import styled, { StyledComponent } from 'styled-components'

export const ImageContainerCircle: StyledComponent<
  'div',
  any,
  {},
  never
> = styled.div`
  width: 100%;
  max-width: 16rem;
  overflow: hidden;
  border-radius: 999rem;
  margin: 0 auto 4rem;
`
