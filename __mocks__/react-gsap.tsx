import React, { ReactNode } from 'react'

export function Tween({ children }: { children: ReactNode }): JSX.Element {
  return <>{children}</>
}

export function Timeline({ children }: { children: ReactNode }): JSX.Element {
  return <>{children}</>
}

export function Reveal({ children }: { children: ReactNode }): JSX.Element {
  return <>{children}</>
}
